What happens when you initialize a repository? Why do you need to do it?
Git initializes empty git repository. It should be done to create a repository to keep track of future commits.

How is the staging area different from the working directory and the repository? What value do you think it offers?
Staging area helps to commit logical changes as a bundle.

How can you use the staging area to make sure you have one commit per logical change?
Add the changed files to staging area of one logical change and commnit the changes.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
Branches are helpful in working on major change without distrubing the main branch and can be merged once the major change get to work perfectly.

How do the diagrams help you visualize the branch structure?
Great.! I understood how the commits are tracked to the parent and how can an unreacheable fix change to be added by changing checking out new branch

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
Merging two branches creates a commit reflecting the changes made in both the branches. It helps understand how the commits are tracked to parent before and after merge of branches.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
automatic merge may include unwanted/redundant code. merging manually gives user to remove the unwanted code and resolve merge conflicts.


